import java.util.*;
import java.util.concurrent.ConcurrentHashMap;

public class test {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        // hasNextInt():当前输入的行下一个被扫描的是否是整数
        // nextInt():获取当前输入的行被扫描出的下一个整数

        // nextLine(): 跳到下一行,返回当前行的字符串
        // hasNextLine(): 是否有下一行输入
//        System.out.println(sc.nextInt());
//        System.out.println(sc.nextLine());
//        System.out.println(sc.hasNextInt());
//        System.out.println(sc.nextLine());
//        System.out.println(sc.hasNextInt());
//        System.out.println(sc.nextInt());
//        System.out.println(sc.hasNextInt());
//        System.out.println(sc.hasNextLine());
        new ArrayList<>();
        Map<String, Object> map = new HashMap<>(2,0.8f);
        ConcurrentHashMap<String, Object> cMP = new ConcurrentHashMap(32);
    }
}
